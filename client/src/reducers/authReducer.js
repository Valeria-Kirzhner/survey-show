export default function (state = {}, action) {// for now = {} becouse it need to be returned and i don't wan't it to be undifiend
    
    switch (action.type) {

        default:
            return state;

     }
}