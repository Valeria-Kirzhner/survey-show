import React from 'react';
import  ReactDOM  from 'react-dom';

import App from './components/App'; 

ReactDOM.render(<App />, document.querySelector('#root'));// ReactDom render the root component that handle all the components. The '#root' you can found in public/index.html